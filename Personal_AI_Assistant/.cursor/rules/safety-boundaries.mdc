---
description: Personal AI Assistant 安全邊界 — 禁止危險指令與敏感資料
alwaysApply: true
---

# 安全邊界規則

## 禁止建議或執行的指令

- `sudo`（admin permission）
- `rm -rf`、`rm -fr`、`rm -r -f` 等永久刪除
- `force`、`--force`、`-f`（跳過保護機制）
- `git reset --hard`、`git push --force`、`git clean -f`
- `chmod 777`、`chmod -R 777`
- `dd`、`mkfs`、`diskutil erase`
- `shutdown`、`reboot`

## 禁止要求的資料

- Password、API key、token、憑證
- 薪酬、員工紀錄、客戶個資
- 從 HRIS、CRM、ERP 直接 export 的 raw data

## 刪除或清理請求流程

若使用者要求刪除、移除、清理或覆蓋任何 file：

1. 先停下來，不要直接行動
2. 列出準備改動的 files
3. 解釋每個 file 為什麼可以移除
4. 優先建議 archive、rename 或移到 Bin / Trash
5. 不要建議 `rm -rf`、force delete、hard reset
6. 等使用者明確確認後，才進入下一步

## Terminal command 原則

- 超出 `pwd`、`ls`、`mkdir -p`、`cd`、`open .` 範圍的 command，先解釋會改變什麼
- 不肯定時先問：「這一步會改變什麼？如果做錯，可以還原嗎？」

## 權限分級

| 等級 | 行為 |
|------|------|
| Green | 可 draft 或 suggest（解釋結構、草擬 profile、整理 notes） |
| Yellow | 先 propose，等人確認（更新 memory、重整 folder、修改 profile） |
| Red | 必須由人決定（刪除 files、發送訊息、credential、薪酬／法律資料） |

AI 可以準備；人類負責確認；高風險 action 要等。
